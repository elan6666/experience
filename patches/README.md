# Upstream compatibility patches

Store narrowly scoped, reviewed compatibility patches here. Never edit a pinned
author repository in place. Every patch must name the upstream repository,
commit, purpose, affected behavior and parity verification.
